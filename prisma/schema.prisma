generator client {
  provider = "prisma-client-js"
}

//prisma schema

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model User {
  id   String @id @default(cuid())
  name String
}
